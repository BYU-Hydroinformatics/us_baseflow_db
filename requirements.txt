# Core scientific stack
pandas>=2.0
numpy>=1.26
scipy>=1.11
pyarrow>=14.0             # Parquet I/O for daily labels & events tables

# Geospatial
geopandas>=0.14
shapely>=2.0

# Baseflow separation
baseflowx>=0.1            # Digital filter methods (Eckhardt, Chapman, LH, etc.)
pybfs>=0.1                # PyBFS non-linear state-space model

# Machine learning
scikit-learn>=1.4         # RF-BFD classifier (from Obj 1)
joblib>=1.3               # Model serialization

# Visualization
matplotlib>=3.8
cartopy>=0.22             # CONUS maps
plotly>=5.18              # Interactive plots

# Utilities
tqdm>=4.66                # Progress bars for batch processing
click>=8.1                # CLI for scripts

# Development / testing
pytest>=8.0
jupyter>=1.0
